{
  "name": "ac-mines-hack",
  "version": "1.0.0",
  "description": "Sistema completo para AC MINES HACK",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",       // Inicia o servidor principal (usado no Render)
    "dev": "nodemon server.js",      // Para desenvolvimento local com reinício automático
    "bot": "node telegram-bot.js"    // Inicia apenas o bot do Telegram
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.64.0",
    "axios": "^1.6.8",              // Alternativa moderna ao request
    "uuid": "^9.0.0"                // Atualizado para evitar avisos de segurança
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
